name: CI/CD for Streamlit App with Frontend

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Lint Python with flake8
      run: |
        pip install flake8
        flake8 . || true

    - name: Check Python formatting with black
      run: |
        pip install black
        black --check . || true

    - name: Set up Node.js for frontend
      uses: actions/setup-node@v3
      with:
        node-version: '16'  # Use the version required by your frontend

    - name: Install frontend dependencies
      working-directory: frontend
      run: |
        npm install

    - name: Build frontend
      working-directory: frontend
      run: |
        npm run build

    - name: Optional – Run Streamlit app test
      run: |
        streamlit run app.py --server.headless true & sleep 10 && pkill streamlit
